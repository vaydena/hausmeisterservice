# Playwright E2E — Test-User Credentials (Demo-Tenant Owner)
# Diese Datei nach `.env.test.local` kopieren und mit echten Werten füllen.
# `.env.test.local` ist gitignored (siehe .gitignore).

E2E_TEST_EMAIL=owner@example.com
E2E_TEST_PASSWORD=set-me
E2E_BASE_URL=http://localhost:3001
